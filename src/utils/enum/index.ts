export enum USER_AGENT  {
 local = "local",
 google = "google"
}
export enum SYS_ROLES{
    user= "user",
    admin = "admin",
    superAdmin = "superAdmin"
}

export enum GENDER {
    male = "male",
    female = "female"
}